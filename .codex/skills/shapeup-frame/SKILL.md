---
name: shapeup-frame
description: Frame raw ideas, feature requests, bug complaints, or vague product concepts before solution design using the Shape Up process. Use when the user wants to start Shape Up framing, says "frame this", has a raw idea, or needs a problem statement, affected segment, business value, appetite, and Frame Go gate before shaping.
---

# Shape Up: Frame for Codex

Use the canonical instructions in `skills/frame/SKILL.md`.

Before acting:

1. Read `skills/frame/SKILL.md` completely.
2. Read the "Read now" references named by that skill.
3. Interpret `/frame` and `/shapeup:frame` as this skill in Codex conversations.
4. Set `SHAPEUP_PLUGIN_ROOT` to the repository root that contains `skills/`,
   `references/`, and `hooks/` when running snippets from the canonical skill.
5. Set `SHAPEUP_PROJECT_DIR` to the user's project root when it differs from the
   plugin root.

Follow the canonical skill exactly after applying those Codex routing rules.
