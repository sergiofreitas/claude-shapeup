---
name: shapeup-build
description: Build a Shape-Go-approved Package with Shape Up execution: orient on the package, work vertically with tests, discover scopes, track RED/GREEN user behaviors, update hill charts, scope hammer, write handovers, and reach Ready to Ship. Use when the user asks to build a shaped feature or continue a Shape Up build.
---

# Shape Up: Build for Codex

Use the canonical instructions in `skills/build/SKILL.md`.

Before acting:

1. Read `skills/build/SKILL.md` completely.
2. Read the "Read now" references named by that skill.
3. Interpret `/build` and `/shapeup:build` as this skill in Codex conversations.
4. Set `SHAPEUP_PLUGIN_ROOT` to the repository root that contains `skills/`,
   `references/`, and `hooks/` when running snippets from the canonical skill.
5. Set `SHAPEUP_PROJECT_DIR` to the user's project root when it differs from the
   plugin root.

Follow the canonical skill exactly after applying those Codex routing rules.
