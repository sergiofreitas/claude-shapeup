---
name: shapeup-shape
description: Shape a Frame-Go-approved problem into a buildable Shape Up Package through codebase analysis, requirements, affordance tables, fit checks, rabbit-hole patching, and Shape Go approval. Use when the user asks to shape a feature, design a solution after framing, or move from Frame Go to a package.
---

# Shape Up: Shape for Codex

Use the canonical instructions in `skills/shape/SKILL.md`.

Before acting:

1. Read `skills/shape/SKILL.md` completely.
2. Read the "Read now" references named by that skill.
3. Interpret `/shape` and `/shapeup:shape` as this skill in Codex conversations.
4. Set `SHAPEUP_PLUGIN_ROOT` to the repository root that contains `skills/`,
   `references/`, and `hooks/` when running snippets from the canonical skill.
5. Set `SHAPEUP_PROJECT_DIR` to the user's project root when it differs from the
   plugin root.

Follow the canonical skill exactly after applying those Codex routing rules.
