#!/bin/bash
# pre-push hook — auto-bump patch version in plugin.json and marketplace.json

REPO_ROOT=$(git rev-parse --show-toplevel)
PLUGIN_JSON="$REPO_ROOT/.claude-plugin/plugin.json"
MARKETPLACE_JSON="$REPO_ROOT/.claude-plugin/marketplace.json"

if [ ! -f "$PLUGIN_JSON" ]; then
  exit 0
fi

# Extract current version from plugin.json
CURRENT=$(grep -oE '"version":\s*"[0-9]+\.[0-9]+\.[0-9]+"' "$PLUGIN_JSON" | grep -oE '[0-9]+\.[0-9]+\.[0-9]+')

if [ -z "$CURRENT" ]; then
  echo "pre-push: could not parse version from plugin.json" >&2
  exit 0
fi

# Split into components
MAJOR=$(echo "$CURRENT" | cut -d. -f1)
MINOR=$(echo "$CURRENT" | cut -d. -f2)
PATCH=$(echo "$CURRENT" | cut -d. -f3)

# Bump patch
NEW_PATCH=$((PATCH + 1))
NEW_VERSION="$MAJOR.$MINOR.$NEW_PATCH"

# Update plugin.json (macOS-compatible sed)
sed -i '' "s/\"version\": \"$CURRENT\"/\"version\": \"$NEW_VERSION\"/" "$PLUGIN_JSON"

# Update marketplace.json if it exists
if [ -f "$MARKETPLACE_JSON" ]; then
  sed -i '' "s/\"version\": \"$CURRENT\"/\"version\": \"$NEW_VERSION\"/" "$MARKETPLACE_JSON"
fi

# Stage and amend the commit
git add "$PLUGIN_JSON" "$MARKETPLACE_JSON" 2>/dev/null
git commit --amend --no-edit --no-verify >/dev/null 2>&1

echo "pre-push: version bumped $CURRENT → $NEW_VERSION"

exit 0
